name: Integration
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  integration:
    uses: ./.github/workflows/integretion_utils.yml
    with:
      network_os: cisco.nxos.nxos
      pytest_addopts: "--color=yes -n 2 --log-level WARNING -vvv --role-excludes=snmp_server"
      environment: integration
