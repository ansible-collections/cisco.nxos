all:
  children:
    nxos_cisshgo:
      vars:
        ansible_connection: ansible.netcommon.network_cli
        ansible_network_os: cisco.nxos.nxos
        ansible_user: admin
        ansible_password: admin
        ansible_host: 127.0.0.1
        ansible_command_timeout: 200
        ansible_ssh_common_args: -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null
        ansible_network_cli_ssh_type: libssh
        ansible_libssh_hostkeys: +ssh-rsa
        ansible_become: false
      children:
        nxos_static_routes_gathered:
          hosts:
            cisshgo-static_routes-gathered:
              ansible_port: 10001
        nxos_static_routes_merged:
          hosts:
            cisshgo-static_routes-merged:
              ansible_port: 10002
        nxos_static_routes_replaced:
          hosts:
            cisshgo-static_routes-replaced:
              ansible_port: 10003
        nxos_static_routes_overridden:
          hosts:
            cisshgo-static_routes-overridden:
              ansible_port: 10004
        nxos_static_routes_deleted:
          hosts:
            cisshgo-static_routes-deleted:
              ansible_port: 10005
