---
# AUTO-GENERATED — promoted from converge_draft.yml with validate-time fixture fixes.

- name: "Nxos_lacp_interfaces — state: gathered"
  hosts: nxos_lacp_interfaces_gathered
  gather_facts: false
  vars_files:
    - ../../../tests/integration/targets/nxos_lacp_interfaces/vars/main.yml
  tasks:
    - name: Gather nxos_lacp_interfaces facts
      register: result
      cisco.nxos.nxos_lacp_interfaces:
        state: gathered
    - name: Assert gathered
      ansible.builtin.assert:
        that:
          - result.changed == false
          - result.gathered is defined

- name: "Nxos_lacp_interfaces — state: merged"
  hosts: nxos_lacp_interfaces_merged
  gather_facts: false
  vars:
    mode: delay
  vars_files:
    - ../../../tests/integration/targets/nxos_lacp_interfaces/vars/main.yml
  tasks:
    - name: Run nxos_lacp_interfaces merged
      register: result
      cisco.nxos.nxos_lacp_interfaces:
        config:
          - name: port-channel10
            links:
              min: 5
          - name: port-channel5
            mode: "{{ mode }}"
            links:
              max: 10
        state: merged
    - name: Assert commands
      ansible.builtin.assert:
        that:
          - result.changed == true
          - result.commands | length == 5
    - name: Idempotent re-run (merged)
      register: idempotent_result
      cisco.nxos.nxos_lacp_interfaces:
        config:
          - name: port-channel10
            links:
              min: 5
          - name: port-channel5
            mode: "{{ mode }}"
            links:
              max: 10
        state: merged
    - name: Assert idempotent (no changes)
      ansible.builtin.assert:
        that:
          - idempotent_result.changed == false
          - idempotent_result.commands | length == 0

- name: "Nxos_lacp_interfaces — state: replaced"
  hosts: nxos_lacp_interfaces_replaced
  gather_facts: false
  vars:
    mode: delay
  vars_files:
    - ../../../tests/integration/targets/nxos_lacp_interfaces/vars/main.yml
  tasks:
    - name: Run nxos_lacp_interfaces replaced
      register: result
      cisco.nxos.nxos_lacp_interfaces:
        config:
          - name: port-channel10
            mode: "{{ mode }}"
            links:
              max: 10
        state: replaced
    - name: Assert commands
      ansible.builtin.assert:
        that:
          - result.changed == true
          - result.commands | length == 4
    - name: Idempotent re-run (replaced)
      register: idempotent_result
      cisco.nxos.nxos_lacp_interfaces:
        config:
          - name: port-channel10
            mode: "{{ mode }}"
            links:
              max: 10
        state: replaced
    - name: Assert idempotent (no changes)
      ansible.builtin.assert:
        that:
          - idempotent_result.changed == false
          - idempotent_result.commands | length == 0

- name: "Nxos_lacp_interfaces — state: overridden"
  hosts: nxos_lacp_interfaces_overridden
  gather_facts: false
  vars:
    mode: delay
  vars_files:
    - ../../../tests/integration/targets/nxos_lacp_interfaces/vars/main.yml
  tasks:
    - name: Run nxos_lacp_interfaces overridden
      register: result
      cisco.nxos.nxos_lacp_interfaces:
        config:
          - name: port-channel11
            mode: "{{ mode }}"
            links:
              min: 4
        state: overridden
    - name: Assert commands
      ansible.builtin.assert:
        that:
          - result.changed == true
          - result.commands | length == 7
    - name: Idempotent re-run (overridden)
      register: idempotent_result
      cisco.nxos.nxos_lacp_interfaces:
        config:
          - name: port-channel11
            mode: "{{ mode }}"
            links:
              min: 4
        state: overridden
    - name: Assert idempotent (no changes)
      ansible.builtin.assert:
        that:
          - idempotent_result.changed == false
          - idempotent_result.commands | length == 0

- name: "Nxos_lacp_interfaces — state: deleted"
  hosts: nxos_lacp_interfaces_deleted
  gather_facts: false
  vars_files:
    - ../../../tests/integration/targets/nxos_lacp_interfaces/vars/main.yml
  tasks:
    - name: Run nxos_lacp_interfaces deleted
      register: result
      cisco.nxos.nxos_lacp_interfaces:
        state: deleted
    - name: Assert commands
      ansible.builtin.assert:
        that:
          - result.changed == true
          - result.commands | length == 5

- name: Parsed state (offline)
  hosts: nxos_lacp_interfaces_gathered
  gather_facts: false
  vars_files:
    - ../../../tests/integration/targets/nxos_lacp_interfaces/vars/main.yml
  tasks:
    - name: Run nxos_lacp_interfaces parsed
      register: result
      cisco.nxos.nxos_lacp_interfaces:
        running_config: |
          interface nve1
            source-interface Ethernet1/100
          interface port-channel10
            lacp min-links 10
            lacp max-bundle 15
          interface Ethernet1/800
            lacp port-priority 100
            lacp rate fast
            description interface
        state: parsed
    - name: Assert parsed result
      ansible.builtin.assert:
        that:
          - parsed | symmetric_difference(result.parsed) | length == 0

- name: Rendered state (offline)
  hosts: nxos_lacp_interfaces_gathered
  gather_facts: false
  vars_files:
    - ../../../tests/integration/targets/nxos_lacp_interfaces/vars/main.yml
  tasks:
    - name: Run nxos_lacp_interfaces rendered
      register: result
      cisco.nxos.nxos_lacp_interfaces:
        config:
          - name: Ethernet1/800
            rate: fast
          - name: Ethernet1/801
            port_priority: 32
            rate: fast
          - convergence:
              graceful: true
            links:
              max: 15
              min: 2
            name: port-channel10
        state: rendered
    - name: Assert rendered commands
      ansible.builtin.assert:
        that:
          - result.rendered | symmetric_difference(rendered) | length == 0
